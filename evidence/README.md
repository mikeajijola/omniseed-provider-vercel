# Acceptance evidence

Deterministic tests use in-memory fake Vercel and HTTP clients. Live observations are read-only and must record only deployment identifiers, public URLs, state, company-binding values, timestamps, and redacted error categories. Credentials and response headers are excluded.
