{
  "name": "@omniseed/vercel-eve-agent-implementation",
  "version": "0.1.0-alpha.0",
  "private": false,
  "description": "Vercel Provider implementation of OmniSeed's agents primitive-family contract using Eve.",
  "scripts": {
    "test": "python3 -m unittest discover -s test -p 'test_*.py'"
  },
  "files": [
    "provider",
    "provider-package.json",
    "provider-configuration.schema.json",
    "README.md"
  ],
  "engines": { "node": ">=22" }
}
