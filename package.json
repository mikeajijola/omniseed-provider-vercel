{
  "name": "@omniseed/provider-vercel",
  "version": "0.1.0-alpha.0",
  "description": "Read-only Vercel connector observation Provider for OmniSeed",
  "type": "module",
  "private": true,
  "files": ["provider-package.json", "provider-configuration.schema.json", "provider/", "evidence/", "README.md"],
  "scripts": { "test": "python -m unittest discover -s test -p 'test_*.py' -v" },
  "engines": { "node": ">=20" },
  "license": "Apache-2.0"
}
